<div class="row">
  <div class="col-lg-12">
    <h1 class="page-header">Calls</h1>
  </div>
<!-- /.col-lg-12 -->
</div>
<div class="row">
  <div class="col-lg-12">
    <div class="panel panel-default">
      <div class="panel-heading">
        <div class="row">
          <form  class="form-inline form-horizontal" role="form">
              <label for="sms_start" class="control-label input-sm">From</label>
              <input class="datetimepicker form-control input-sm" id="sms_start" name="sms_start" type="datetime" value="<%= params[:sms_start] %>"/>
              <label for="sms_end" class="control-label input-sm">To</label>
              <input class="datetimepicker form-control input-sm" id="sms_end" name="sms_end" type="datetime" value="<%= params[:sms_end] %>"/>
              <button class="btn btn-primary btn-sm" type="submit">Filter</button>
          </form>
       </div>
     </div>

      <div class="table-responsive">
        <table class="table table-striped table-bordered table-hover dataTable" id="dataTable">
          <thead>
            <tr role="row">
              <th><%=  link_to_sorted "At", :at %></th>
              <th><%=  link_to_sorted "Route", :route_id %></th>
              <th><%=  link_to_sorted "Destination", :destination %></th>
              <th><%=  link_to_sorted "Duration", :duration %></th>
              <th><%=  link_to_sorted "Cost", :price_for_customer %></th>
              <th><%=  link_to_sorted "IP", :ip %></th>
            </tr>
          </thead>
          <tbody>
            <% @smss.each do |sms| %>
            <tr class="odd gradeX">
              <td><%= localize(sms.at, :format => :long) if sms.at.present? %></td>
              <td><%= sms.route %></td>
              <td><%= sms.destination %></td>
              <td><%= sms.duration_hhmmss %></td>
              <td><%= sms.price_for_customer %></td>
              <td><%= sms.ip %></td>
            </tr>
            <% end %>
           </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
<div class="row">
  <div class="col-sm-12">
    <%= paginate @smss, theme:"turbobil" %>
   </div>
</div>
<script>
  jQuery('.datetimepicker').datetimepicker();
</script>

