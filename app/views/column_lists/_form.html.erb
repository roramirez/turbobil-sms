<%= form_for [@list, @column_list] do |f| %>
  <% if @column_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@column_list.errors.count, "error") %> prohibited this column_list from being saved:</h2>

      <ul>
      <% @column_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <%= f.text_field :name, autofocus: true, class: 'form-control', as: 'string' %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
