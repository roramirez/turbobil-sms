<div class="row">
  <div class="col-lg-12">
     <h1 class="page-header">
       Lists
       <div class="pull-right">
        <%= link_to 'New List', new_list_path, class: 'btn btn-default' %>
        </div>
     </h1>
  </div>
</div>

<div class="row">
  <div class="col-lg-12">

    <div class="panel panel-default">

      <div class="table-responsive">
        <table class="table table-striped table-bordered table-hover" id="dataTable">
          <thead>
            <tr>
              <th colspan="3"></th>
            </tr>
          </thead>

          <tbody>
            <% @lists.each do |list| %>
            <tr>
              <td><%= link_to 'Edit', edit_campaign_path(list) %></td>
            </tr>
            <% end %>
         </tbody>
      </table>
    </div>
  </div>
</div>
<div class="row">
  <div class="col-sm-12">
    <%#= paginate @campaigns, theme:"turbobil" %>
   </div>
</div>
